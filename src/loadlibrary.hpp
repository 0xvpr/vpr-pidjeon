#ifndef LOAD_LIBRARY_HEADER
#define LOAD_LIBRARY_HEADER

#include "definitions.hpp"

unsigned load_library_a(const types::resource&, const types::injector&);
unsigned load_library_w(const types::resource&, const types::injector&);

#endif // LOAD_LIBRARY_HEADER
